# CNN baseline under the common protocol (same split, input pipeline, optimiser, epochs).
experiment: baseline_googlenet
output_dir: runs
model:
  backbone: googlenet
  pretrained: true
loss:
  name: ce
train:
  epochs: 20
  optimizer: adam
  lr: 1.0e-4
