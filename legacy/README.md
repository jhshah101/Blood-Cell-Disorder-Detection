# Legacy material

`Software_code.ipynb` is the original ResNet-50 baseline notebook released with the first submission, kept unchanged for the record. Its behaviour, and that of the original `WBC_Without.py` and `main.py`, is documented as runnable configurations in `../configs/legacy/`. It is superseded by `configs/baselines/resnet50.yaml` and should not be used for new results.
